# Job Finder - Python Dependencies
# Prefer: uv sync (from project root)
# Fallback: pip install -r requirements.txt (in a venv)

ollama>=0.3.0
google-genai>=1.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
requests>=2.31.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
jinja2>=3.1.0
typer>=0.9.0
